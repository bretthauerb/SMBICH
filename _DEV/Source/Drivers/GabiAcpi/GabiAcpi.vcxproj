<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" ToolsVersion="17.0"
         xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- ========================================================= -->
  <!-- Project configurations                                   -->
  <!-- ========================================================= -->
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64">
      <Configuration>Debug</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
  </ItemGroup>

  <!-- ========================================================= -->
  <!-- Source files                                              -->
  <!-- ========================================================= -->
  <ItemGroup>
    <ClCompile Include="Device.c" />
    <ClCompile Include="Driver.c" />
    <ClCompile Include="Queue.c" />
  </ItemGroup>

  <ItemGroup>
    <ClInclude Include="..\..\_SHARED\inc\AcpiGabi.h" />
    <ClInclude Include="Device.h" />
    <ClInclude Include="Driver.h" />
    <ClInclude Include="Queue.h" />
    <ClInclude Include="resource.h" />
    <ClInclude Include="Trace.h" />
  </ItemGroup>

  <ItemGroup>
    <Inf Include="GabiAcpi.inf" />
  </ItemGroup>

  <!-- ========================================================= -->
  <!-- Globals                                                   -->
  <!-- ========================================================= -->
  <PropertyGroup Label="Globals">
    <ProjectGuid>{8CF74B16-2EFC-45AB-833C-3987CDE67171}</ProjectGuid>
    <TemplateGuid>{497e31cb-056b-4f31-abb8-447fd55ee5a5}</TemplateGuid>
    <MinimumVisualStudioVersion>17.0</MinimumVisualStudioVersion>
    <Configuration>Debug</Configuration>
    <Platform Condition="'$(Platform)' == ''">x64</Platform>
    <RootNamespace>GabiAcpi</RootNamespace>
  </PropertyGroup>

  <!-- ========================================================= -->
  <!-- Disable obsolete InfVerif (WDK 26100+)                    -->
  <!-- ========================================================= -->
  <PropertyGroup>
    <RunInfVerif>false</RunInfVerif>
  </PropertyGroup>

  <!-- ========================================================= -->
  <!-- Standard C++ props                                        -->
  <!-- ========================================================= -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

  <!-- ========================================================= -->
  <!-- Configurations                                            -->
  <!-- ========================================================= -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'" Label="Configuration">
    <UseDebugLibraries>true</UseDebugLibraries>
    <PlatformToolset>WindowsKernelModeDriver10.0</PlatformToolset>
    <ConfigurationType>Driver</ConfigurationType>
    <DriverType>KMDF</DriverType>
    <DriverTargetPlatform>Universal</DriverTargetPlatform>
    <Driver_SpectreMitigation>Spectre</Driver_SpectreMitigation>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'" Label="Configuration">
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>WindowsKernelModeDriver10.0</PlatformToolset>
    <ConfigurationType>Driver</ConfigurationType>
    <DriverType>KMDF</DriverType>
    <DriverTargetPlatform>Universal</DriverTargetPlatform>
    <Driver_SpectreMitigation>Spectre</Driver_SpectreMitigation>
  </PropertyGroup>

  <!-- ========================================================= -->
  <!-- Standard C++ props                                        -->
  <!-- ========================================================= -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <!-- ========================================================= -->
  <!-- *** REQUIRED WDK DRIVERKIT PROPS (26100) ***               -->
  <!-- ========================================================= -->
  <Import Project="$(WDKContentRoot)build\$(WindowsTargetPlatformVersion)\Microsoft.Windows.DriverKit.props" />

  <ImportGroup Label="PropertySheets">
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props"
            Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" />
  </ImportGroup>

  <!-- ========================================================= -->
  <!-- Build settings                                            -->
  <!-- ========================================================= -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <DebuggerFlavor>DbgengKernelDebugger</DebuggerFlavor>
    <IncludePath>..\..\_SHARED\inc;$(IncludePath)</IncludePath>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
    <DebuggerFlavor>DbgengKernelDebugger</DebuggerFlavor>
    <IncludePath>..\..\_SHARED\inc;$(IncludePath)</IncludePath>
    <SignMode>Off</SignMode>
  </PropertyGroup>

  <!-- ========================================================= -->
  <!-- Compilation                                               -->
  <!-- ========================================================= -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'">
    <ClCompile>
      <WppEnabled>true</WppEnabled>
      <WppKernelMode>true</WppKernelMode>
      <GuardEHContMetadata>true</GuardEHContMetadata>
      <TreatWarningAsError>false</TreatWarningAsError>
    </ClCompile>
  </ItemDefinitionGroup>

  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'">
    <ClCompile>
      <WppEnabled>true</WppEnabled>
      <WppKernelMode>true</WppKernelMode>
      <AdditionalOptions>/Qspectre %(AdditionalOptions)</AdditionalOptions>
      <GuardEHContMetadata>true</GuardEHContMetadata>
    </ClCompile>
  </ItemDefinitionGroup>

  <!-- ========================================================= -->
  <!-- Targets                                                   -->
  <!-- ========================================================= -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />

  <!-- ========================================================= -->
  <!-- *** REQUIRED WDK DRIVERKIT TARGETS (26100) ***             -->
  <!-- ========================================================= -->
  <Import Project="$(WDKContentRoot)build\$(WindowsTargetPlatformVersion)\Microsoft.Windows.DriverKit.targets" />

</Project>